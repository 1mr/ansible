{% for user in proftpd_users %}
{{ user.name }}:{{ user.password|password_hash('md5') }}:{{ user.uid|default( proftpd_uid ) }}:{{ user.gid|default( proftpd_gid ) }}::{{ user.home }}:{{ user.shell|default('/bin/false') }}
{% endfor %}
