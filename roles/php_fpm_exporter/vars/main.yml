---
go_arch_map:
  x86_64: 'amd64'
  aarch64: 'arm64'

go_arch: "{{ go_arch_map[ansible_facts['architecture']] | default(ansible_facts['architecture']) }}"

systemd_lib_directory: /etc/systemd/system
systemd_default_directory: /etc/default
